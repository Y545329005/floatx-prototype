# prototype 项目常驻规则

## 数据校验优先于 UI 反馈

- 页面跳转/提示「成功」「审核中」之前，必须校验数据操作的真实返回值；保存失败（`return false`、异常、未入队）时禁止展示成功类页面。
- 反例：`submitKyc` 静默丢弃提交后仍 `navigate('kyc-submitted')`，制造"假象页"（2026-09-20 修复）。

## 多处存储必须登录/初始化时对账

- 同一事实同时存在于内存态（如预设对象）与持久层（如快照数组）时，登录/初始化必须从持久层显式回读，禁止两处各用各的。
- 反例：`testAccounts` 预设状态与 `kycSubmissions` 快照脱节，后台"待审核"与 APP 侧"要求重填"互相矛盾（2026-09-20 修复）。

## 身份切换必须同步 mock 桥梁

- 任何切换 currentUser 的路径（注册、手动登录、登出、快捷登录、测试钩子）必须调用 `setMockCurrentUser`（登录后含 `restoreKycStatus`），保证 data.js 模块级单例与 React state 一致；新增登录路径时先核对桥梁调用。
- 反例：`__resetMockLogin` 曾漏调桥梁（已修）；`registerUser` / LoginPage 手动登录至今未接桥梁，登录后门控/状态页读到残留账号（2026-09-20 发现，未修）。

## 权限/认证流变更先穷举拦截点

- 修改认证门控、审核流、资格门槛前，先列出四层拦截点并逐层标注"改 / 不改 + 理由"：① 路由门控白名单（App.jsx `KYC_ONLY_PATHS`）② 各页面入口 UI 显示条件 ③ 数据层校验 ④ 下游消费端门控（防越权兜底）。
- 反例：PI 入口断点——文案承诺"可进行 PI 认证"但入口 UI 三处未同步（2026-09-20 修复）；解耦时靠四层清单确认"放行 kyc-pi 后申购仍被兜底拦截"，零漏洞交付。
